# Environment variables for Maan's Store

# Enable Firebase storage (set to 'true' to use Firebase instead of in-memory storage)
FIREBASE_ENABLED=true

# Node environment
NODE_ENV=development

# Database URL (for PostgreSQL if needed in the future)
# DATABASE_URL=your_postgresql_url_here
